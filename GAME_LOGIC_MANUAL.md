# 《迟灯杂货铺》游戏逻辑说明书

本文档记录当前纯 JavaScript 版本的实际游戏规则、数据结构和页面行为。用途包括：

- 为玩家说明书提供规则底稿；
- 为后续开发避免破坏现有逻辑；
- 让 AI 或其他开发者能够据此制作移植版、重构版或续作；
- 明确区分“已经实现的功能”和“仍在规划中的内容”。

当前审计日期：2026-08-17。本文已按现有源码重新核对；正文不再保留“清晨自动掉落”和“玩家翻牌选客”等旧流程。

## 0. 当前核心循环：五时段与主动探索

每一天依次分为早上、中午、下午、傍晚、深夜。每个时段只能完成一次主要行动：

1. 选择中央的迟灯杂货铺：留店营业。普通日先判断本时段是否无人来访；若门铃响起，再从符合章节、日期、季节、月份、月相和天气条件的候选中，叠加角色时段权重、客群时段倾向、好感与装修加成，自动随机选出一位来客。节日营业会改为抽取一组不同来客，并逐位接待。
2. 选择野外地点：立刻关店。主角从当前位置沿蓝色道路网络前往目的地，在那里度过整个时段并采集；抵达后留在该地点。下一次选择其他地点或小店时，必须继续沿道路移动，不能跨格跳转。该时段玩家本人不会接待客人。

地图始终是首页主屏。来客抵达时，候选信息、人物故事、心情、需求、报价与交易按钮会在地图上方以模态接待窗口弹出；制作可留在窗口继续操作，交易、拒绝或完成简历来访后窗口关闭，再通过结果卡进入下一时段。深夜行动结束后才进入下一天，并进行明日装修与陈列、员工、工资、天气和章节结算。刷新页面不会重复采集、重新选择已经出现的客人或推进时段。

每个时段开始时，当前合法客池会预先分散显示在不同地图地区。出生地区由客群倾向与“日期＋时段＋角色名”的稳定散列决定，因此同一时段刷新不会乱跳；所有头像保持彩色，已结识者显示姓名，未结识者用问号隐藏姓名。点击开门后仍先经过空店判定与原权重抽选：未被抽中的候选淡出，被抽中的一位从出生地区沿外部蓝色道路移动到中央小店，抵达后才打开紧凑的居中交易弹窗；地图仍透过遮罩可见。候选展示不把任何角色变成必然来客。

开门并不保证来客。基础空店概率约为 6%；冬季清晨、雾天清晨、夏季晴热下午、雨夜，以及风暴中的傍晚或深夜会提高空店概率，最高不超过 28%。第一章空店概率减半，避免新手期过于冷清；迎宾员工会轻微降低空店概率。没有来客时仍会消耗当前时段，并在地图记录和逐日总结中写明原因。

地图包括马斯河岸、低地圩田、旧农场、瓦登海边、石南林地和废弃风车。18 种材料各有可配置的地点、时段、季节、天气、月相、权重和稀有度条件。所有已配置条件同时满足后，材料才进入采集池；有候选材料时至少获得一种，并可能获得第二种，常见材料还有机会一次取得两件。

地图上的每个地点会直接显示当前环境与时段下实际进入采集池的材料及稀有度，不再使用独立信息栏；没有候选材料时显示“此刻无已知收获”。材料卡也会显示来源地点、具体环境和稀有度；成品卡会显示完整配方，并按总耗材与稀有材料数量标记为基础、进阶或困难。24 种成品只使用这 18 种材料。

每位角色拥有独立的五时段权重，并叠加所属客群的时段倾向。时段只调整概率，不构成绝对禁止，因此任何单一角色都不会因为时段加成达到 100% 必然出现。

旧存档迁移时保留日期、库存、铜币、关系、简历、员工、装修、配方解锁和日志；旧版未完成的自动掉落与翻牌状态会被清除，并从当天早上的地图开始。

地图道路与主角移动共用同一组百分比路径坐标。人物必须沿道路图寻找连通路径；例如某些地点之间必须经由小店中转。抵达采集块后，会从多条内部小路中随机选择一条巡游，碰撞散落材料后才结算收获；时段推进后人物仍停留在目的地。手机端采用地图优先视图：首页大标题收起，经营模式压成细条；月相、季节、月份、星期、天气和预报压进一条可横滑环境栏，五时段也压成单行。地图高度根据手机可视区域调整；玩家或来客开始沿路线移动前，页面会自动将整张地图居中，保证下排地区与采集动画不会落到屏幕外。

深夜时段结束后不会直接修改日期，而是打开每日经营小结。小结按早上至深夜列出五次行动、来客和结果，并汇总来客数、外出次数、采集件数与铜币净变化。玩家确认小结后才执行下一日结算；如果新一天同时触发周薪结算，随后继续显示工资总结。

雇佣人数不再设置上限。每位角色的简历新增独立采集能力，包括两个擅长地区、每时段采集上限和偏好材料。入职后可分配为采集员，并从其擅长地区中指定目的地；地图会显示各地点当前被派遣的员工。

每个时段结束时，当班采集员会与玩家行动并行结算。玩家留店时，采集员仍会外出；玩家外出时，只要至少一名非采集岗位员工留店，小店仍然营业。留店员工会自动接待一名符合条件的客人：库存足够则自动成交并增加 1 点好感，库存不足则记录未成交；若玩家和所有员工都外出，该时段闭店。员工采集与代班结果会进入当日日志和每日总结。

### 0.1 前期材料平衡

第一章的 6 种基础成品只使用运河玻璃瓶、代尔夫特银线、风车旧木片、风车铜齿轮和寒鸦羽毛。这 5 种材料都有不受季节、天气和月相限制的全年地图来源，因此不会出现整个春季无法制作第一章委托物品的情况。

### 荷兰主题内容规范

- 三类材料分别称为“低地匠作”“圩田馈赠”“北海异象”；18 种材料都绑定运河、风车、代尔夫特、郁金香花田、弗里斯兰、瓦登海或北海等本地意象。
- 24 种成品保留原有配方难度，但名称和用途改为荷兰语境；代表物包括运河水面木屐、风车捕风瓶、代尔夫特真心镜与灰雁归巢铃。
- 希尔达是一位弗里斯兰马访客，灰雁邮差、寒鸦、赤狐和獾等动物来自荷兰常见或代表性动物意象。
- 顶部导航不再提供店铺和预报 Tab。第二章后，通过首页地图中央小店的“店内管理”进入原店铺页面。首页环境栏保留独立的“当前天气”信息格，其右侧另设“天气预报”按钮；第五章后才能点击进入预报页面。进入管理或预报都不消耗时段。地图上的店铺使用两个完全独立的按钮：“开门营业”是高亮的大型主要按钮并推进当前时段，“店内管理”是低调的小型次按钮，点击店铺卡片空白处不会触发营业。
- 店铺可购买代尔夫特彩绘木屐、会转的小风车、弗里斯兰马摆件和灰雁风向标。它们有独立动画，也会参与访客来访权重与心情计算。
- 已入职且当日工作的员工会以头像显示在首页地图。非采集岗位留在小店；切换为采集员或更改采集地点时，头像沿对应路线移动到目的地，随后留在当地播放采集微动画，不返回店铺。调回非采集岗位时才沿路线回店。手机地图由六块地区和一块中央小店组成七块互不重叠的不规则拼图：上排两块、中排“地区—小店—地区”三块、下排两块；板块间仅留窄巷供路线交错穿行。
- 旧存档中的原料、成品及“露特鹿女士”会自动迁移到新名称，库存数量、已解锁商品、货架与角色关系不会因改名丢失。

第二章开始逐步加入自然馈赠与异象材料。石南萤光粉、圩田日光灰、琥珀郁金香球茎、弗里斯兰霜绒、北海雾盐、运河悬雨、风车风结、北海月光片、山墙影尘和瓦登星砂保留符合主题的高概率环境来源，同时增加低概率全年备用来源。主环境与备用来源同时满足时，采集权重叠加，地图材料标签显示“加成”。正确规划环境仍然有价值，但错误季节不再造成绝对卡死。

难度按商品章节递增：第一章委托固定只需求 1 件，拒绝也不会降低好感；第二章非付款型委托通常需求 1 件，心情差时最多 2 件；第三章起恢复完整心情数量规则，需求为 1–3 件。第一章配方只消耗 2–3 种常见材料，第二章开始混入少见材料并维持较短配方，第三章保留多材料与稀有环境配方。新游戏额外携带 1 根寒鸦羽毛作为首次制作缓冲。

材料卡会反向列出它能制作的全部成品；寒鸦羽毛还会注明看店黑猫和部分员工周薪用途。地图材料标签的悬停说明也包含主要用途。

## 1. 游戏定位

玩家经营一家位于荷兰低地小镇的奇异杂货铺。每天有五个可行动时段；玩家在营业、外出采集和店内管理之间安排时间。普通营业可能无人来访，也可能从当前合法客池中自动抽取一位客人；节日营业则可能迎来一组客人。玩家可以制作或出售客人需要的商品，也可以拒绝交易。

除自由游戏外，游戏具有三类失败条件：小店总坚固度归零、连续 7 个完整游戏日没有成功交易、信用归零后 7 日内未恢复。五种开局模式还提供不同年度或剧情胜利目标；胜利后可以继续经营。长期目标包括：

- 收集材料并发现商品配方；
- 认识 36 位访客；
- 提升访客好感；
- 根据天气预报、访客喜好和可能来客调整店铺；
- 购买并布置陈设；
- 通过经营日志观察长期结果；
- 随章节推进解锁新的经营能力。

## 2. 技术与存档

- 技术栈：原生 HTML、CSS、JavaScript，无构建工具和服务器依赖。
- 托管目标：GitHub Pages 等静态站点。
- 存档位置：浏览器 `localStorage`。
- 存档键名：`late-lantern-save`。
- 数据只保存在当前浏览器和当前设备。
- 刷新页面不会推进日期，也不会重新抽取当日天气或访客。
- 清除浏览器站点数据会丢失存档。
- 首页底部提供“清空本地数据，重新开店”。执行前使用阻塞式确认框说明删除范围；确认后重建第一日状态，并强制回到经营模式选择，选定模式后才正式开始。

## 3. 页面结构

### 3.1 来客

首页以地图为主屏，显示：

- 当前章节与进度；
- 木框仪表盘形式的日期、季节、月份、星期、月相和天气；
- 当前时段与各地点当前可采材料；
- 中央小店的“开门营业”和“店内管理”独立入口；
- 营业后系统自动选出的实际来客，或本时段无人来访的结果；
- 客人的故事、需求、报价、好感与心情；
- 出售、现场制作和拒绝按钮；
- 当日截至目前由玩家和员工取得的材料；
- 店内库存速览；
- 清空本地存档入口。

来客信息使用地图上方的接待弹窗；完成交易、拒绝、简历或录用处理后关闭弹窗，再推进到下一时段。深夜结束后先显示逐日总结，确认后才进入下一天。

首页与客簿统一使用 5 颗心表示 0～10 点好感。每增加 1 点填充半颗心，每增加 2 点填满一颗心；心形右侧保留精确的 `x/10` 数字。

每次点击“开门营业”时，系统先进行无人来访判定；通过后，才从符合章节、日期、环境和时段的候选访客中按权重随机抽取一位。玩家不再通过翻牌选择客人。只有实际来客确定后才会：

- 将客人写入已结识名单；
- 显示姓名、头像、故事与交易条件；
- 解锁其需求商品配方；
- 开放出售、现场制作、拒绝和录用操作。

### 3.2 货架

显示所有原材料与已发现商品。

- 原材料从一开始即可查看，但会受章节字段影响后续扩展。
- 商品只有在有客人首次提出需求后才被发现。
- 第 4 章后，可以随时主动制作已发现商品。
- 第 6 章后，可以随时安排下一天要摆到柜台或收起的成品。
- 最多同时展示 3 种成品。
- 原材料不能展示。
- 展示中的成品售罄后自动撤下。

### 3.3 客簿

按六个访客类别展示 36 位访客：

- 普通人类；
- 兽客；
- 夜行异客；
- 远方旅人；
- 精怪与自然灵；
- 星辰与神秘存在。

未解锁章节的访客显示为锁定。已解锁但从未出现的访客显示问号。见过至少一次后显示姓名、头像、类别、好感、完成来访次数和成交次数。

顶部导航不再提供客簿 Tab。首页“本时段门铃”区域右上角提供客簿入口；客簿页顶部提供“返回地图”，返回时恢复进入前的首页滚动位置。客簿按六种来客大类折叠，默认展开第一类。角色卡首层只显示头像、姓名、五心好感、关系阶段、来访数和成交数；来访规律、稀有度、喜恶、关系高光与求职状态收纳在角色的“查看资料”区域。详细档案按下表逐级解锁：

| 好感 | 解锁内容 | 当前实际作用 |
|---:|---|---|
| 0 | 基础身份、统计 | 不显示来访规律 |
| 1 | 季节与月份规律 | 没有限制时明确显示“没有固定季节” |
| 2 | 星期规律 | 没有限制时明确显示“没有固定星期” |
| 3 | 天气与月相规律 | 没有限制时明确显示“没有固定天气或月相” |
| 4 | 好感门槛带来的追加来访窗口 | 显示访客配置中的高好感规则 |
| 5 | 稀有度 | 第 1 章访客为常见，第 2 章为少见，第 3 章为稀有 |
| 6 | 喜欢的天气、陈设和展示商品 | 内容来自 `VISITOR_MOODS` 正数配置 |
| 7 | 不喜欢的天气、陈设和展示商品 | 内容来自 `VISITOR_MOODS` 负数配置 |
| 8 | 独立“关系高光” | 卡片获得特殊描边、星光和高光说明，与普通好感条区分 |
| 9 | 永久进阶与求职状态 | 此后普通购物永久提高支付；每次来访都有角色独立概率递出简历 |
| 10 | “羁绊已满” | 收下简历时升至满级；下一次来访可预付首周工资并雇佣 |

### 3.4 我的店铺

第 2 章解锁。

- 陈设可以在任何阶段购买，购买后永久拥有。
- 已购买陈设可以随时加入或移出“明日布置”，不必等客人离开。
- 调整只在进入下一天时复制为生效布置，不会改变正在接待的客人。
- 收起的陈设不显示、不提供温暖度、不影响来访权重，也不影响报价。
- 已购买但收起的陈设不会丢失。

当前六件陈设：

| ID | 名称 | 基础作用 |
|---|---|---|
| `lamp` | 萤火吊灯 | 吸引夜行异客与星辰访客 |
| `plant` | 会打哈欠的盆栽 | 吸引兽客与自然灵 |
| `rug` | 星河地毯 | 吸引远方旅人，增加基础报酬 |
| `cat` | 看店黑猫 | 强烈吸引兽客 |
| `clock` | 十三点钟 | 强烈吸引人类 |
| `teaset` | 雾气茶具 | 增加所有访客基础报酬 |

每件陈设还会根据具体访客的独立喜恶产生正面、负面或无影响。类别吸引效果和个人喜恶是两套独立逻辑。

### 3.5 明日预报

第 5 章解锁，显示下一天的：

- 游戏日编号；
- 星期；
- 季节与月份；
- 月相；
- 2～3 种候选天气及各自概率。

第 7 章后增加明日潜在访客。第 8 章后增加已结识访客的部分喜恶提示。

### 3.6 经营日志

选定经营模式后立即开放。开局即可看到当前模式目标与对应的进度摘要。第 3 章（第 15 天）解锁详细经营分析，包括：

- 当前经营天数；
- 当前月份；
- 总来客次数；
- 已结识访客数量；
- 成功交易次数与成交率；
- 已结识访客的平均好感；
- 每位客人的出现次数、成交次数、成功率和好感；
- 默认折叠、按时间倒序排列的环境、交易、拒绝和章节事件。

## 4. 每日流程

1. 新一天读取日期、季节、月份、星期、月相，以及前一天预报中抽出的实际天气；应用已经计划好的陈设和成品展示。
2. 从早上开始，玩家在每个时段选择营业或一个采集地点。店铺与货架管理不消耗时段。
3. 营业时先判断空店，再计算合法客池和权重并自动选客；外出时根据当前地点寻路到目的地，并从当前合法材料池中至少取得一种材料。
4. 来客出现后计算心情、实际需求与报酬；玩家可出售、反复现场制作至数量足够，或拒绝。简历及录用使用各自操作。
5. 每个时段结束时，所有在岗采集员并行采集；若玩家外出且店里存在非采集员工，员工还会自动代班营业。
6. 早上、中午、下午、傍晚依次推进；深夜结束后显示五时段经营小结。
7. 玩家确认小结后进入下一天，处理工资、欠薪、入职、员工制作、陈列员安排、灾害、章节和失败／胜利检查。

陈设与最多三件柜台商品可以在任何时刻调整为“明日方案”。第 4 章后的货架主动制作也可以随时使用。进入下一天时，明日方案才成为实际生效布置，因此不会追溯修改当天已经抽出的客人、心情或交易条件。

### 4.1 雇员与简历

- 好感第一次达到 9 后，角色进入永久进阶状态并写入独立名单；以后即使当前好感因拒绝交易暂时降到 9 以下，也不会退出进阶状态。只要尚未入职且当前没有待处理简历，每次实际来访都会独立判定是否递出简历，并非必定发生。
- 判定没有递简历时仍是普通购物来访，但永久提高支付：铜币基础报酬额外增加 35%（至少 2 枚），物品报酬额外增加 1 件。该效果不会因为一次交易或一次未录用而消耗。
- 简历来访没有购物需求。收下简历会把好感升到 10，并永久放入简历库；不收简历则以后继续随机判定。
- 收到简历后的下一次本人来访开始，角色恢复为正常高价购物来访，同时额外显示录用按钮。玩家可以正常交易、现场制作或拒绝，也可以预付简历上的首周工资选择录用。
- 当次没有选择录用不会让简历失效。只要角色尚未入职，之后每次来访都可以继续正常交易并再次选择是否录用；员工名额已满或暂时付不起工资也不会删除简历。
- 确认录用后不会当天到岗。所有员工统一在之后的下周一正式入职；若确认日正好是周一，则在 7 天后的周一入职。到岗前占用一个员工名额，但不提供职位效果。
- 员工总数不设统一上限；只有“低地最会经营的小店”模式把同时在职员工限制为 3 位。入职角色从普通访客池和预报名单移除。
- 员工职位可随时在“我的小店”切换为迎宾、制作者、陈列员或采集员，但角色不能选择其能力配置明确禁用的岗位。迎宾增加对应客群信任；制作者按个人每日上限自动补货但每种成品库存最多 2 件；陈列员按个人策略安排下一日最多三件展示；采集员在两个擅长地区中选择驻地，每个时段并行采集。
- 每位角色都有独立周薪、每年休假月和该月中的休假周。休假周不提供职位效果；“休假月”就是休假周所在的那个月，该月整月不能辞退。
- 周薪不限定为铜币，可以要求铜币或某一种材料。简历、员工卡和录用界面都会显示准确的资源与数量，首周工资和续付工资使用同一种资源。
- 工资每 7 天预付下一周。非休假月无法结清工资与欠薪时，员工自行离职；休假月无法支付时按对应工资资源记为欠薪，不会立即离职。
- 获得对应工资资源或保存状态时优先补休假欠薪。休假月结束后，下一次发薪若仍不能结清欠薪和当周工资，员工离职。
- 确认录用和在职期间，简历仍保留在内部记录中。只有角色入职后被辞退或因欠薪离职，旧简历才失效。因为永久进阶状态仍保留，之后重新进入“高价购物或随机递简历”模式，必须收到新简历后才能再次录用。
- 旧简历失效时，该角色的简历版本号增加。下一次随机递交的新版本会把周薪数量小幅增加，并轮换休假周；每两次失效还会移动一次休假月。因此重新雇佣同一角色时，玩家需要重新评估是否负担得起。

### 4.2 每周工资总结

- 完成星期日并进入周一时，自动显示独立的周工资总结层。
- 总结列出当前每位员工、职位、约定周薪、休假月与休假周、本次为下周实际支付的资源数量、欠薪和离职状态。
- 首周工资若已在录用时预付，会明确显示“本次未扣款／首周工资此前已预付”。
- 没有员工时也会显示空账本，确认后进入本周正常游戏。
- 总结状态保存在本地；刷新页面不会反复结算工资，玩家确认账目后本周不再弹出。

### 4.3 角色工作能力与职位

每位角色的简历配置包含独立岗位能力，简历库和在职员工卡都会完整显示。不能胜任的职位不会出现在其职位选择中。

- **迎宾**：所有迎宾至少使普通客人的信任心情 `+1`。每位角色另有两个擅长接待的客群，对这些客群提供 `+1～+3` 的专长信任加成。例如兽客员工通常更擅长兽客与自然灵，星辰员工更擅长星辰与夜行异客。
- **制作者**：部分角色不能制作；其余角色每天最多自动制作 1 或 2 件，而不是把所有商品一次补满。普通型按货架顺序选择，高阶型优先材料更复杂的配方，稀有型优先章节等级更高的商品。单种成品库存上限仍为 2 件。
- **陈列员**：第三个职位。部分角色不能陈列，其余角色每天自动摆放 2 或 3 种有库存的成品。普通型按库存顺序，熟悉型优先常见商品，稀有型优先高章节商品，预报型会读取下一天的天气候选、可能来客和已配置商品喜好，选择综合好感得分最高的商品。

陈列员在日期推进前处理“明日方案”，随后该方案成为新一天实际陈列；制作者在新一天材料掉落后工作；迎宾只在客人心情结算时工作。所有职位在员工实际休假周和正式入职前都不产生效果。

## 5. 日历

当前日历实现：

- 7 天为一周；
- 28 天为一个月；
- 3 个月为一个季节；
- 12 个月、336 天为一个现有程序年份；
- 四季顺序：春、夏、秋、冬。

星期由全局天数循环计算。

### 5.1 月相

月相每 28 天循环：

- 第 1 与第 28 夜：新月组；
- 第 2～13 夜：盈月组；
- 第 14～16 夜：满月组；
- 第 17～27 夜：亏月组。

视觉上细分为新月、娥眉月、上弦月、盈凸月、满月、亏凸月、下弦月和残月。

### 5.2 荷兰节日

节日按照游戏内的 12 个 28 日月循环，每个游戏年重复：

| 节日 | 游戏日期 | 节日营业人数 |
|---|---:|---:|
| 低地新年 | 1 月 1 日 | 2～3 位 |
| 国王节 | 4 月 27 日 | 3～4 位 |
| 解放日 | 5 月 5 日 | 2～4 位 |
| 圣尼古拉斯节 | 12 月 5 日 | 3～4 位 |
| 圣诞节 | 12 月 25～26 日 | 2～4 位 |

节日当天首页会显示对应主题横幅、颜色、图案和轻量飘落动画，地图也会获得节日视觉强调。节日提示不需要章节解锁。

节日开门不执行普通的空店判定，而是从当前时段合法、尚未入职且能通过现有店铺部件进入的访客池中，按原有来访权重进行不重复抽取。人数受合法客池数量限制，因此极端情况下可能少于节日标称人数。被选中的客人会一起沿地图路线来到店铺，然后进入接待队列：玩家逐位交易、制作、拒绝或处理简历，最后一位处理结束后才推进至下一时段。中间刷新页面会保留尚未处理的节日队列。

节日一整个时段在逐日总结里仍只占一个营业行动，但“营业来客”会按实际队列人数统计，避免挤乱五时段时间轴。自动玩家使用同一个队列和交易流程，会自动逐位处理。

普通自动玩家会检查今天与明天是否为节日。发现节日后，会从高权重潜在来客中选出最多五类主要需求，把相应成品库存目标提高到最多 3 件，并提高对应配方材料的采集权重。工资材料、修缮木料和生存储备规则仍然优先，因此不会为了节日备货透支工资或必要维修资源。AI 实验室快速模式保留完全相同的节日抽客、交易和计分，但跳过来客错峰移动、节日横幅重绘和所有动画等待。

## 6. 章节

当前程序实现 52 章，每章持续 7 天。前 8 章带功能解锁：

| 章节 | 起始日 | 名称 | 功能 |
|---|---:|---|---|
| 1 | 1 | 初燃的灯 | 基础接待、交易与现场制作 |
| 2 | 8 | 雾中的街 | 解锁我的小店 |
| 3 | 15 | 星落之夜 | 解锁逐客统计与完整逐日记录；基础模式日志开局即开放 |
| 4 | 22 | 会自己排列的货架 | 打烊时主动制作商品 |
| 5 | 29 | 门铃响了两次 | 解锁明日天气预报 |
| 6 | 36 | 没有寄件人的信 | 解锁最多三件成品展示 |
| 7 | 43 | 雨水记得名字 | 预报明日潜在访客 |
| 8 | 50 | 影子来买东西 | 显示已结识访客的喜恶 |

达到新章节起始日时，会写入日志并显示解锁弹窗。第 9～52 章目前只推进文字剧情，不追加经营功能；章节推进不会阻断每日接待。

### 6.1 第 9～52 章文字剧情

第 9～52 章已经写入程序，依次为：被遗忘的橱窗、上一任店主、倒着开放的花、春天最后的委托、春末灯市、不肯落下的太阳、瓶中的海潮、借来的名字、会做梦的商品、第十三把钥匙、门后的车站、永远误点的列车、售票员的账本、没有终点的旅人、太阳的碎屑、被偷走的黄昏、站台上的店主、夏末长昼祭、同一本故事、乌鸦的收藏室、写错的结局、迟到二十年的回信、不存在的第六位客人、月亮背面的字、店主不能完成的愿望、被交换的记忆、空白的客簿、她曾经来过、黄昏收藏家、拒绝出售的代价、秋末无月夜、第一场没有声音的雪、封在冰里的门铃、星星开始熄灭、上一任店主的房间、写给小店的委托、第四枚灯芯、被归还的商品、五十二个空格、黄昏收藏家的愿望、世界遗忘迟灯巷、今夜不做生意、最后一位客人、灯亮到明天。

第 9 章从第 57 日开始，此后每 7 日进入下一章；第 52 章从第 358 日开始。完成第 52 章后，章节保持为第 52 章，但日期、天气、掉落、来客、交易和打烊循环会无限继续。

### 6.2 年历与章节周的关系

游戏年历仍为 12 个 28 日月，即 336 日；章节使用连续 7 日周，因此第 49～52 章会进入下一游戏年的春季。这是当前明确采用的规则，而不是阻塞条件。若未来改为每季增加一个“季隙周”，必须同步检查：

- 月份与季节计算；
- 年份计算；
- 访客日程；
- 天气权重；
- 月相是否继续独立按 28 天循环；
- 章节起始日；
- 旧存档迁移。

## 7. 天气与预报

天气类型 ID：

- `clear`：晴朗；
- `rain`：雨；
- `fog`：雾；
- `wind`：风；
- `storm`：夏季表示雷暴，冬季视觉表现为降雪。

每个季节具有不同天气权重。

### 7.1 预报生成

每天生成并保存下一天的预报：

1. 读取下一天所在季节的天气池。
2. 按季节权重无放回抽取 2 或 3 种天气。
3. 2 种或 3 种的选择本身带随机性，目前约 58% 概率生成 3 种。
4. 将候选天气原始权重归一化为总和 100% 的显示概率。
5. 预报写入存档，刷新不会变化。

### 7.2 次日实际天气

结束一天后，实际天气只从前一晚保存的候选天气中按权重抽取。因此：

- 预报不会指出唯一结果；
- 每个候选结果都不是 100%；
- 实际天气一定在预报名单中；
- 玩家可以依据风险而不是确定答案进行布置。

## 8. 主动采集与环境材料池

游戏不会在清晨自动赠送材料。每次玩家或采集员工到达一个地点时，使用地点、当前时段、季节、天气和月相建立该地点的合法材料池。

1. 只有配置条件全部满足的规则参与本次地点材料池。
2. 同一材料同时命中基础来源和环境来源时合并权重，并标记“环境加成”。
3. 玩家采集时，只要池中有材料就先按权重保证取得一种；随后可能追加第二种，常见材料可能一次取得两件。
4. 员工每个时段按个人地区能力、产量上限和偏好材料加权取得一种材料及对应数量。
5. 玩家和员工的收获立即进入库存、当日收获列表、时段记录与经营日志，并立即尝试优先补发相同资源的休假欠薪。
6. 地图没有合法材料时才允许出现“没有收获”；有合法材料时不能结算为空。
7. 地区内部材料以散落图标显示，人物从多条内部路线中随机巡游。实际路线保证穿过至少一个本次收获；多名员工的路线与拾取动画可以并发。

## 9. 访客出现逻辑

共有 36 位访客，分为六类，每类六位。原有 24 位构成前三章的核心阵容；新增 12 位从第 4～6 章逐步加入，每个大类增加两位，避免稀释开局来访池。

新增阵容的岗位平衡采用定向配置，而不是完全依赖序号生成：六个采集地区各有两位第一专长角色，同时补充预报型陈列员、稀有优先制作者、每日两件制作者及不同客群的高强度迎宾。工资同时覆盖铜币和多种常见／少见材料；角色支付也在铜币与材料之间分散，避免形成单一资源循环。

### 9.1 日程规则

每位访客有一个 `schedule` 数组。数组内各窗口是“或”关系；单个窗口中的多个条件是“且”关系。

可用条件：

- `weekday`；
- `season`；
- `month`；
- `moon`；
- `weather`；
- `favor` 解锁门槛。

好感达到门槛后会解锁额外来访窗口，而不是覆盖旧窗口。

### 9.2 每个营业时段的实际抽取

普通日每次开门先进行空店判定，再筛选满足日期、季节、月份、月相、天气、章节与好感规则的访客，并叠加个人与客群时段权重抽取一位。员工代班营业使用同一合法客池和空店判定。节日由同一合法客池和权重进行不重复的多人抽取，并按队列逐位接待。

基础权重：`10`。

权重附加：

- 好感权重：每 1 点好感增加 `+0.6` 基础抽选权重，10 点时最多 `+6`。因此每次提升都有作用，但满好感角色的基础权重最多从 10 增至 16，不会形成必然来访；
- 当前摆出陈设的类别吸引权重，累计最多 `+6`；
- 角色自身五时段权重与所属客群时段倾向作为乘数参与。

任何访客都不会因为加成达到 100% 必定出现。当前第一章基础日程已保证所有日历与天气组合至少存在两位候选访客。

### 9.3 明日潜在访客

第 7 章后，预报页面会使用明天的日期、季节、月份、月相和所有候选天气计算访客并集。

- 只要访客能在任意一种候选天气下满足日程，就进入名单。
- 已经见过的访客显示身份。
- 未见过的访客显示问号。
- 名单不是实际结果；第二天每次营业仍会重新进行空店判定和加权抽取，一天可以因多次营业接待多位客人。

## 10. 商品与制作

材料和成品分开管理。

### 10.1 配方发现

- 原材料目录可预先显示。
- 成品只有在访客首次索要后才被发现。
- 未发现成品不能制作。

### 10.2 现场制作

客人正在等待时，如果成品数量不足但单件配方材料齐全，可以点击当前订单的制作按钮。每次点击制作一件，材料立即扣除、成品立即加入库存。挑剔型或混合型客人可能需要 2～3 件，因此必须反复制作到库存达到当日实际需求后才能出售。

### 10.3 主动制作

第 4 章后，可以随时从货架主动制作任意已发现且材料齐全的商品。每次点击制作一件。

## 11. 柜台展示

第 6 章后开放。

- 可以随时调整明日方案；
- 只能展示实际库存大于零的成品；
- 最多展示三种成品；
- 展示不会消耗商品；
- 同一种商品只占一个展示位；
- 展示商品可能让具体访客喜欢或不喜欢；
- 展示品售罄后自动撤下；
- 进入下一天时才复制为实际展示，当天客人不受临时修改影响。

## 12. 访客心情与报价

36 位访客各自拥有独立的 `VISITOR_MOODS` 配置。配置包含：

- 对天气的喜恶；
- 对六件陈设中部分陈设的喜恶；
- 对部分展示成品的喜恶。

数值通常为 `-2`、`-1`、`+1` 或 `+2`。没有配置的对象代表无影响。

每位访客还拥有一种 `tradeStyle` 交易性格：

| 类型 | 需求数量 | 付款变化 |
|---|---|---|
| `payment` 议价型 | 始终需要 1 件 | 使用完整心情付款加成 |
| `quantity` 挑剔型 | 心情好 1 件、平静 2 件、心情差 3 件 | 心情不改变付款 |
| `both` 混合型 | 心情好 1 件、平静 2 件、心情差 3 件 | 使用一半强度的心情付款加成 |

三种类型都保证心情好时对玩家更有利：议价型支付更多；挑剔型消耗更少；混合型同时减少需求并提高付款。心情差则产生相反结果。需求数量最低为 1，不会出现零件订单。

### 12.1 心情因素

当日心情分由以下内容相加：

1. 当前天气影响；
2. 当前摆出陈设的个人喜恶；
3. 当前展示商品的个人喜恶；
4. 好感加成：`min(3, floor(好感 / 3))`；
5. 每日稳定随机波动：`-1`、`0` 或 `+1`，其中 `0` 出现更频繁。

最终心情分限制在 `-5` 至 `+7`。

每日随机波动由游戏日与访客姓名计算，因此同一天刷新页面不会改变结果。

### 12.2 报价计算

先根据交易性格取得付款敏感度：

- `payment`：`1`；
- `quantity`：`0`；
- `both`：`0.5`。

铜币报酬：

`心情付款变化 = round(心情分 × 2 × 付款敏感度)`

`最终数量 = max(1, 基础数量 + 陈设基础报酬 + 心情付款变化)`

物品报酬：

`心情付款变化 = round(心情分 / 2 × 付款敏感度)`

`最终数量 = max(1, 基础数量 + floor(陈设基础报酬 / 2) + 心情付款变化)`

所有付款最低为 1。页面会同时显示交易性格、实际需求数量和最终付款。

第 8 章前，首页会显示总体心情与报价变化，但不会解释具体原因。第 8 章后才显示天气、陈设、商品、好感和随机情绪的正负标签。

章节解锁与客簿好感解锁是两套不同信息层：第 8 章控制首页是否解释“今天为什么改变报价”；单个访客好感 6、7 控制客簿是否永久列出该访客通常喜欢或不喜欢的事物。未见过的访客在任何页面都不会显示喜恶。

### 12.3 布置生效时点

陈设与展示始终可以编辑“明日方案”，但今日实际生效方案不会改变。因此玩家可以在任何页面整理店铺，却不能看到客人后再临时改变这位客人的心情或条件。

## 13. 交易与好感

### 13.1 出售

条件：库存中拥有客人需要的成品。

结果：

- 按当日心情和交易性格扣除 1～3 件成品；
- 获得当日最终报价；
- 好感 `+2`，最高 10；
- 成交次数 `+1`；
- 完成来访次数 `+1`；
- 写入日志；
- 结果弹窗可直接进入下一天。

### 13.2 拒绝

结果：

- 好感 `-1`，最低 0；
- 完成来访次数 `+1`；
- 不增加成交次数；
- 写入日志；
- 结果弹窗可直接进入下一天。

### 13.3 出现次数

日志统计会把当前尚未处理的访客也视为已经出现一次。完成交易或拒绝后使用持久化的 `visits` 数值，不会重复累计。

## 14. 动画与反馈

首页根据天气显示：

- 晴天光尘；
- 雨丝；
- 流雾；
- 风痕与叶片；
- 雷暴雨幕与闪光；
- 冬季飘雪。

每日首页内容使用轻量入场动画。视觉布局上，日期环境位于顶部，候选访客和实际访客居中，今日掉落移到交易区之后并与库存速览组成连续的信息区域。

店铺陈设动画：

- 吊灯摇摆与呼吸发光；
- 盆栽打哈欠；
- 地毯星纹闪烁；
- 黑猫来回巡视；
- 时钟钟摆摇动；
- 茶具冒出蒸汽。

系统遵循 `prefers-reduced-motion`。用户要求减少动态时，会停止或大幅缩短循环动画。

## 15. 核心存档字段

| 字段 | 含义 |
|---|---|
| `day` | 当前游戏日 |
| `coins` | 铜币数量 |
| `inventory` | 材料与成品库存映射 |
| `history` | 日志事件数组 |
| `relationships` | 每位访客的好感、来访和成交统计 |
| `decor` | 已购买陈设 ID |
| `activeDecor` | 当前摆出的陈设 ID |
| `plannedDecor` | 下一天准备摆出的陈设 ID |
| `displayedProducts` | 当前展示的成品名，最多三个 |
| `plannedDisplayedProducts` | 下一天准备展示的成品名，最多三个 |
| `applications` | 已收到的长期有效角色简历；入职后继续保留，辞退或欠薪离职时删除 |
| `employmentRevisions` | 每个角色旧简历失效次数，用于生成下一版工资和休假条件 |
| `weeklySummary` | 最近一次周一工资总结、员工条目及是否已确认 |
| `advancedVisitors` | 曾经达到好感 9、永久进入高价购物与求职判定的角色名 |
| `employees` | 员工的职位、采集驻地、录用日、周一入职日、士气、罢工、欠薪和下次发薪日等状态 |
| `discoveredItems` | 已发现商品配方 |
| `metCustomers` | 至少出现过一次的访客名 |
| `environmentDay` | 当前环境对应的游戏日，用于防止刷新重新抽取天气 |
| `weather` | 当日实际天气 |
| `dailyDrops` | 当日掉落结果 |
| `tomorrowForecast` | 下一天预报日与候选天气 |
| `timeSlot` | 当前五时段索引：早上至深夜为 0～4 |
| `playerLocation` | 主角当前所在地图节点，决定下一次寻路起点 |
| `dailyActions` | 当天已经完成的五时段行动、收获与员工并行结果 |
| `dailySummaryOpen` | 深夜结束后的逐日总结是否正在等待确认 |
| `resolved` | 当前时段来客是否已处理 |
| `currentCustomer` | 当前时段来客在访客数组中的索引 |
| `currentVisitType` | `shop` 或 `resume`，防止刷新改变购物／简历来访类型 |
| `visitorRevealed` | 当前时段是否已有实际来客，防止刷新重抽或提前交易 |
| `gameMode` / `modeStats` | 当前经营模式及年度收入、满意度、员工峰值、灾害、罢工和胜利记录 |
| `survival` / `gameOver` | 信用、连续无交易、挽救期和失败状态 |
| `fortress` | 店铺各部件当前／最大耐久、已安装加固与连续平静天数 |

读取旧存档时：

- 缺少 `activeDecor` 的旧存档会把所有已购买陈设视为正在摆出；缺少明日方案时复制当前方案；
- 当前与明日展示商品都会过滤无效名称并限制为三件；
- 旧存档缺少雇员、简历或来访类型时使用空员工状态和普通购物来访；
- 缺少明日预报时会立即生成并保存一份。

## 16. 配置文件职责

### `data/items.js`

包含：

- 三类材料定义；
- 18 种材料；
- 24 种成品及配方；
- 四季天气权重。

### `data/exploration.js`

- 六个采集地点、五时段与外部道路图；
- 18 种材料的地点、时段、季节、天气、月相、基础权重、稀有度与备用来源；
- 新手期全年可得的基础材料保障。

### `data/visitors.js`

包含：

- 36 位访客；
- 访客类别；
- 故事、需求和基础付款；
- 来访日程；
- 每位访客的天气、陈设和商品喜恶。
- 36 位访客与独立头像文件的映射。
- 每位访客的 `payment`、`quantity` 或 `both` 交易心情类型。
- 每位访客的工资资源与数量、休假月、休假周、递简历概率，以及迎宾、制作、陈列、采集四类能力。

### `assets/visitors/` 与 `assets/icons/`

- `assets/visitors/` 保存 36 张独立角色头像；
- 未见过的访客仍由程序显示问号，不提前加载到可见 UI；
- `assets/icons/` 保存 1024、512、192 和 Apple Touch 尺寸的游戏图标；
- `manifest.webmanifest` 使支持的浏览器可以将游戏作为独立 Web App 添加到主屏幕；
- 两张未裁切角色图集和游戏图标母版保存在 `assets/source-art/`，便于以后重新切图或制作其他尺寸。

### `game.js`

包含：

- 状态与存档迁移；
- 日历、章节和页面解锁；
- 天气、预报、主动采集池与每时段来客抽取；
- 制作、交易、好感、心情和报价；
- 店铺购买与摆放；
- 简历、雇佣、职位、周薪、休假、欠薪与自动制作；
- 页面渲染、动画触发和事件监听。

### 其他运行模块

- `player-route.js`：覆盖玩家外出流程，按道路图从当前地点寻路、驻留目的地并结算主动采集。
- `gather-map-ui.js`：地图材料提示、地区内部多路线、散落材料、玩家与多员工并发巡游及碰撞拾取表现。
- `employment-fixes.js`：员工在岗／待入职／休假判断、欠薪到账优先补发及员工采集结算修正。
- `inventory-ui.js`：货架分类、筛选和配方可读性增强。
- `disaster-system.js`：灾害、部件耐久、修补、加固、预警与损坏拒客。
- `loss-system.js`：坚固度、连续无交易、信用三类失败及失败纪念总结。
- `mode-workforce.js`：五种模式、年度胜利、顾客满意度、员工关系、士气、效率与罢工。
- `autoplay.js`：可视化自动玩家的完整决策与 UI 操作。
- `ai-telemetry.js`：本地 AI 行为日志、摘要与 JSON 导出。
- `ai-lab.js`：本地快速批量模拟、策略变异、排序、前三名保留和策略采用／写入。
- `ai-policy-defaults.js`：发布版本按模式读取的默认 AI 权重；本地采用权重优先于该文件。
- `local_server.py`：仅监听本机的开发服务器，允许实验室只改写 `ai-policy-defaults.js`，不会自动暂存、提交或推送。

脚本按 `index.html` 中的顺序加载，后置模块会包装或覆盖前面同名函数。判断“当前实际规则”时必须以最终加载后的函数链为准，不能只依据 `game.js` 中较早保留的兼容实现。

## 16.1 破坏性天气与店铺坚固度

- 破坏性天气从普通天气结果派生，实际灾害一定与当天真实天气相符。
- 第五章解锁预报页以前，玩家看不到破坏性天气预警；解锁后可看到明日综合风险、可能灾害和可能受损部位。
- 灾害池包括北海暴风、运河暴雨、积雪重压、雷击与低地漫水。
- 灾害风险从第一章约 22% 起步，随章节和平静天数逐渐提高，最高约 65%。
- 每个无灾害日都会累计平静值并额外提高约 4% 风险；连续平静 4 天后，下一份预报保证包含恶劣天气，次日会从预报名单中实际落下一种并触发破坏。
- 单次灾害只破坏一个部位，伤害从早期约 3～7 点逐渐提高，后期最高 38 点。
- 小店有店门、窗户、屋顶、电路、防水层五个独立部件；总坚固度是所有部件当前耐久之和。
- 每个部件有三阶加固。前两阶可以选择昂贵铜币安装或材料兑换；最高阶只能使用材料。
- 日常修补恢复最多 12 点耐久，可消耗 1 个风车旧木片或 8 枚铜币。
- 部件耐久归零后，不同类别的客人可能到访但拒绝进店，仍会记录一次到访及离店原因。
- 全店总坚固度归零后无法开门营业；非自由模式会触发失败结局，自由模式仍可从地图进入店内管理并修理。
- 灾害、受损、加固、修补和损坏拒客都会写入经营日志。
- 相关状态保存在 `state.fortress`、`state.tomorrowHazard` 和 `state.todayDisaster` 中。
- 灾害实际发生时会暂停当前视觉流程，播放全屏震动、闪光和风雨冲击演出，并用强提示明确显示灾害名称、受损部件、本次损伤与剩余耐久；同一场灾害只提示一次，刷新页面不会漏掉尚未确认的提示。

### `disaster-system.js`

包含灾害配置、部件耐久、加固目录、修补支付、预报展示、换日伤害、地图坚固度提示与损坏拒客规则。

## 17. 修改规则时的检查清单

新增访客时检查：

- 名称唯一；
- 类别有效；
- 需求商品存在；
- 付款物品存在；
- 日程至少有一个窗口；
- `VISITOR_MOODS` 中存在同名配置；
- 解锁章节有效。

新增商品时检查：

- 所有配方材料存在；
- 至少有访客提出该商品；
- 商品章节不晚于首位需求访客；
- 喜恶配置中的商品名拼写一致。

新增陈设时检查：

- ID 唯一；
- 购买成本存在；
- 场景位置与动画适配桌面和手机；
- `VISITOR_MOODS` 引用使用相同 ID；
- 收起时不会残留视觉或效果。

修改日循环时检查：

- 刷新不会重复掉落；
- 刷新不会重新抽天气或访客；
- 今日来客不会读取同日修改的明日布置；
- 已处理的访客不能重复结算；
- 次日天气来自前一晚预报；
- 结束一天后才推进日期。

## 18. 自动玩家

- 首页右下角提供“普通自动玩家”按钮，可随时开始或暂停；暂停后玩家继续使用同一份状态手动操作。新游戏的模式选择框也提供“让自动玩家选择并开始”，但开启、暂停、重新开始与清空存档始终只由真人触发。
- 自动玩家不会跳过游戏逻辑，而是依次高亮并点击现有地图、交易、制作、拒绝、弹窗、日结和周结 UI，因此移动、采集、来客、灾害及结算动画都会正常显示。
- 普通性格会在能满足潜在需求时优先营业；缺少材料时根据已解锁配方、当前库存、地点产出与稀有度选择采集地点；无可采资源时回店营业。
- 第四章起，自动玩家可以使用与真人相同的货架主动制作按钮。它根据预报来客需求补货，并保留工资材料、修缮木料和基础资源缓冲。
- 第二章起，自动玩家会在保留工资、修缮铜币与关键材料后购买少量高收益陈设。它会结合明日潜在来客的类别与个人喜恶建立目标布置，主动摆出合适陈设，也会收起不合适的旧陈设；目标数量随经营时间缓慢增加。
- 第六章起，自动玩家会从有库存的已发现成品中建立最多三件目标柜台，结合明日潜在来客喜好、需求和商品等级主动摆放、收起及替换。这些操作和玩家手动布置一样，到下一天才生效。
- 来客出现后依次考虑录用、出售、现场制作和拒绝。可负担且员工较少时会录用，简历来访会优先收下。
- 小店耐久低于约 35% 时会优先使用当前可负担的方式修缮；坚固度归零会立即停止自动流程并进入失败结局。
- 自动状态不写入存档。刷新页面后默认为暂停，防止玩家失去控制。
- 普通自动玩家采用稳健策略：任一部件低于约 78% 时尝试预防性修缮，并结合明日灾害风险及可能受损部位选择加固；消费前会保留工资、基础材料、修缮木料和铜币缓冲。
- 自动玩家按模式设置主动招聘目标：精益经营 3 位、店员之家 8 位、风暴/故事/自由模式 4 位。至少完成 4 笔交易、信用不低于 3，并备好首周工资、下一周续薪与应急库存时便可录用；连续 4 天无交易时暂缓扩编。未录用但简历仍有效时，思考提示会明确说明缺少哪项条件。
- 未达到招聘目标时，陈设评分会额外偏向能够吸引“简历已在库”或好感达到 7 以上客人所属类别的物件，增加候选人再次来访或达到投递门槛的机会，但不会跳过正常来访随机。
- 自动玩家拥有完整员工管理权：按能力选出迎宾、制作者和第六章后的陈列员，其余员工安排采集；采集员会依据配方缺口、工资资源、地点产出和个人特长切换采集地点。非休假月中，长期欠薪且低士气、极低效率或即将无法续薪的员工可能被辞退。
- 自动玩家可以自行选择开局模式并处理年度胜利后的继续经营。游戏失败、重新开始、清空数据、开启自动玩和暂停接管仍属于真人控制层，不由自动玩家触发。
- 连续无交易达到 3 天后提高目标商品及其配方材料权重；达到 4 天后，若没有可出售或可制作的商品，会放弃例行营业时段并集中采集，直到具备完成交易的条件。
- 营业判断使用候选池的加权成交覆盖率，而不是“至少有一位能成交”。连续无交易 1、3、5 天后，开店所需覆盖率依次提高到约 50%、72%、90%；危险期会同时为 3～5 种高权重需求备货，避免只准备一种商品后反复赌随机来客。
- 客人现场制作、货架主动制作、购买、修缮和加固统一使用资源安全检查。尤其是材料工资会预留约两周用量，AI不能再在发薪前把工资材料消耗为商品。
- 危机期仍永久保护材料工资，但普通材料的额外缓冲会释放，修缮木片缓冲由 3 件降至 2 件，允许AI使用最后一份非工资材料完成救命交易。
- 连续 2 天无交易后暂停购买陈设和调整次日布置；连续 3 天后暂停预防性修缮与新增加固，只有最弱部件低于约 35% 时才抢修。危机解除后恢复正常投资。

## 19. 失败条件与店铺信用

- 小店所有部件的当前耐久总和降至 0 时立即判负。若由当天灾害造成，会先播放灾害受损演出，再显示游戏结束画面。
- 每个完整游戏日结束时检查交易；连续 7 天没有玩家或员工完成成功交易，触发“灯火熄灭”结局。任意成功交易会将连续计数清零。
- 店铺信用初始及上限为 5 点。员工因无法结清工资、且不受休假月保护而离职时，每位扣除 1 点信用；每次成功交易恢复 1 点。
- 信用降至 0 后进入 7 个完整游戏日的挽救期。期间成功交易使信用恢复到 1 以上便解除危机；期限结束仍为 0 时判负。
- 如果信用挽救期与连续无交易在同一天同时达到 7 天，优先显示“失去信用”结局，使两类经营失败保持可区分。
- 游戏结束状态写入本地存档，自动玩家立即暂停，所有结局都显示经营天数、来客次数、成功交易和最后员工数量。重新开始必须经过二次确认并清空本次本地进度。
- 失败结算同时生成“这家小店曾经很明亮”高光纪念：抵达章节、最佳客缘、结识与满好感人数、配方数、员工峰值、灾害与加固、最终坚固度和模式收入；并从经营日志摘录第一笔成交、第一位伙伴、最远章节及加固里程碑。旧失败存档也会按现有状态即时生成。
- 旧存档不会根据历史日志倒推无交易天数或信用；更新后从当前日开始累计。但加载时总坚固度已经为 0 的旧存档会立即进入店铺毁坏结局。

## 20. 开局经营模式与年度胜利

- 没有模式记录的新存档会在开局显示一次不可跳过的模式选择。模式写入本地存档，本局不可切换，重新开店后可重新选择。
- 首页“清空本地数据”采用同一开局流程：清空并重建第一日后立即暂停操作、显示模式选择；只有选定模式并写入开局日志后才能开始行动。
- “低地最会经营的小店”：同时在职员工硬上限为 3；每 336 天结算一次，要求年度经营收入至少 220 点、全年员工峰值不超过 3、年末坚固度至少 80%、无彻底损坏部件、无欠薪且信用至少 4。
- 经营收入把铜币按 1 点计入，收到的材料按基础估值折算，玩家交易和员工代售都会计入。
- “店员之家”：员工数量无上限；年末至少 8 位员工、平均士气至少 60、无欠薪且信用至少 4。平均士气 70/80 分别作为幸福团队与金色高光评价，不影响基础过关。
- “风暴守望者”：灾害综合风险额外增加 18%，最高仍限制为 85%；一年内挺过至少 8 场灾害、年末坚固度至少 80%且没有彻底损坏部件即获胜。
- “低地故事屋”：客池独占增加“沉睡圩田的白鹿”和“北海长明灯夫人”。两人分别要求较高章节、结识人数、满好感人数、信用和坚固度，并叠加稀有季节、月相与天气日程；结识两人后立即获胜。
- “自由游戏”：没有年度胜利目标，三类强制失败条件不生效；灾害、损坏、工资、信用、员工关系和罢工仍正常运行。
- 年度目标达成后显示胜利总结并永久记录本局已获胜状态，但不会结束存档，玩家可继续下一年度刷新经营结果。
- 首页目标卡持续显示当前模式与关键进度；每日经营总结会追加该模式当日最新目标数字；选择模式及年度胜利都会写入经营日志。
- 经营日志顶部的四张摘要卡随模式变化：精益经营突出年度收入、贡献最高来客、员工峰值与房屋状态；店员之家突出人数、平均士气、罢工欠薪与岗位；风暴模式突出灾害、总坚固度、最弱部件与加固；故事模式突出特殊来客、结识人数、满好感人数和下一位隐藏角色的前置条件。自由游戏保留通用经营统计。
- 模式收入按来客分别累计，因此精益经营日志可以显示本年度为小店贡献经营价值最高的角色；年度结算后收入与来客贡献榜会随新年度重新开始。

## 21. 员工关系、士气与罢工

- 每位可雇佣角色拥有喜欢共事者、回避共事者、不喜欢的天气和在意的店铺部件。关系由角色配置稳定生成并保存在角色规则层，可继续改为手工剧情配置。
- 员工士气范围为 0～100，新员工默认为 75。工资正常、喜欢的同事会提高士气；欠薪、讨厌的同事、厌恶天气、灾害、低信用和在意部件低于 45%会降低士气。
- 士气低于 60 时，按时发薪的每日恢复由 +2 提高为 +4，帮助正常经营的团队回到及格线。重复发生的关系惩罚按每日结算：讨厌同一工作地点的同事 −3、厌恶天气 −3、灾害 −5、危险部件 −7、低信用 −5；欠薪仍是严重惩罚 −18。喜欢同事每日 +2。
- 好恶只在员工处于同一工作地点时生效：非采集岗位都视为在店内，采集员必须被分配到同一地点。
- 士气与同事关系共同形成 55%～130% 的工作效率，实际影响迎宾强度、采集数量、每日制作上限和陈列数量。
- 士气低于 40 会显示警告；低于 25 后每天可能罢工，士气越低概率越高但最高 75%。罢工持续 1～3 天，期间员工仍在名册、照常计薪，但不会产生任何职位效果。
- 员工卡显示士气、效率、关系与最近影响原因；罢工员工在地图显示罢工标识和动画。

## 22. AI 决策训练日志

- 自动玩家启用时会在独立的 `late-lantern-ai-training-v1` 本地存储中创建一次观察会话；不显示在游戏 UI，也不会上传网络。
- AI 的复盘、比较和规则调优均在本地进行。发布版本只更新经过验证的决策规则，不捆绑、不上传玩家的训练轨迹。
- 每次决策保存决策前快照、操作类型、稳定目标标识、AI显示的理由、操作后快照与资源差值。快照包括模式、日期时段、章节、季节月相天气、铜币、非零库存、坚固度、信用、无交易天数、员工职位/士气/欠薪/罢工、当前来客需求、成交、模式收入和灾害数。
- 会话记录暂停、游戏失败和模式胜利结果，可用于比较同一AI在不同模式下的存活天数、成交、收入、房屋与信用表现。
- 为避免占满浏览器存储，只保留最近 8 次自动会话，每次最多 350 个决策步骤；游戏清档不会删除训练轨迹，便于跨局比较。
- 控制台接口为 `LateLanternAIData.summary()`（简表）、`get()`（完整对象）、`export()`（下载 JSON）和 `clear()`（单独清除AI日志）。
- 游戏失败结算页提供“导出本局 AI 决策日志”按钮，供没有开发者控制台的 IDE 内置浏览器直接下载；导出仍是纯本地操作。

## 23. 已验证的不变量

当前版本自动或脚本验证过：

- 36 位访客均有心情配置；
- 36 位访客均有雇佣配置，所有物品工资都引用有效材料；
- 所有心情配置引用的陈设和商品均存在；
- 所有日历与天气组合至少存在两位第一章候选访客；
- 单个访客不会获得 100% 来访概率；
- 明日预报始终包含 2～3 种天气；
- 预报概率总和为 100%；
- 次日实际天气属于前一晚预报名单；
- 最多只能展示三种成品；
- JavaScript 文件通过语法检查；
- 项目适用于静态服务器和 GitHub Pages。
- 各采集地区本身显示为微型场景：合法材料散落于左上、右上、中央、左下等位置，地区内部常驻多条棕色或绿色小路，与地区之间的蓝色城镇道路区分。人物进入后随机选择一种内部巡游路线；系统会让路线依次穿过本次实际收获，再接回随机路线模板，因此只要地区存在合法材料就至少碰到一种。未碰到的候选材料会留在场景中作为可见机会。员工驻场工作也使用同一巡游与碰撞采集表现。该表现层不绕过季节、天气、月相和时段规则。
- 采集地区卡片只在左上角保留紧凑的图标与地点标题，说明文字和员工姓名条不占用场景面积。多名采集员可以在相同或不同地区同时工作；每位员工使用独立的场景层、随机路线和拾取状态，动画并发播放而非排队结算。
- 第 8 章结束后会在第 57 日进入第 9 章，第 52 章结束后仍可继续逐日营业。
- 手机端天气层固定在可视区域，滚动页面不会让雨、雪等效果脱离屏幕。
# 本地 AI 实验室与经营高分（2026-08-17）

- “低地最会经营的小店”保留原年度过关条件：员工峰值最多 3 位、年度经营收入至少 220、年末坚固度至少 80%、无彻底损坏部件、无欠薪且信用至少 4。批量策略排名先比较是否过关；同为通过或同为未通过时，再追求游戏结束时的钱箱余额最大化。
- 一年仍为 12 个月 × 28 天 = 336 天。因此第 108 天处于第一年，不是第二年。
- `localhost` / `127.0.0.1` / `file:` 环境会显示“AI 实验室”；线上 GitHub Pages 不显示入口。
- 实验室可选择五种经营模式，默认连续运行 10 局、每局最多 336 天，提前失败则立即记录。批量过程隐藏地图、来客弹窗、移动与天气表现层，只运行完整规则和决策逻辑。不同模式使用各自的硬目标与排序指标；“最会经营”先看是否通过原年度目标，再按最终钱箱排序。
- 快速实验开始后进入独立的后台实验看板：不执行滚动、高亮、走路等待、采集等待或 AI 逐步日志写入，也不显示任何游戏弹窗。看板显示当前局数和模拟天数，并在每局结束后追加该局结果；10 局完成后进入完整分析表。
- 实验看板同时显示当前在职员工与员工峰值、房屋总坚固度与最弱部件、信用、连续无交易天数、年度经营收入和钱箱。快速模式只在内存中推进存档；每个游戏日结束都会更新看板并主动让出一次浏览器绘制帧，不需要手动刷新。每局结束才持久化实验结果。
- 实验看板始终提供“暂停/继续”“结束并查看已有结果”“重新运行 10 局”三个控制。暂停状态写入本地，页面重载后不会擅自恢复；结束实验会保留已经完成的局并返回结果表。
- 快速模式停用天气粒子、来客候选预览、客簿、日志、明日预报、店内速览与导航解锁等不参与 AI 决策的 DOM 渲染，只保留交易、地图、库存制作、店员和修缮所需的最小交互状态。实验控制按钮在桌面与手机上始终保持单排。
- 每批结束会按所选模式的排序规则保存前三名策略，并强制停留在 AI 实验室结果页。下一批保留三名精英策略，再围绕它们的营业覆盖率、招聘储备、修缮阈值和装修招募权重生成七个变体；各模式的精英池分别保存在本地。
- “店员之家”使用独立的初始策略族与迭代基因：招聘目标 8–11 人、开始招聘所需成交数、最低招聘信用、危机中止招聘阈值、现有团队工资储备周数、低士气辞退阈值和招募装修权重。年度 pass 的团队士气线为平均 60；满意度优化和不亏损安全线另行排序。
- AI 分配店内岗位时会同时比较能力与双向好恶，避免把互相讨厌的员工长期安排在同一店内工作；安排采集地点时也会把同地点员工的双向关系加入评分，在产出需求相近时优先选择更和谐的组合。
- “店员之家”的优化主指标改为顾客满意度，原年度 pass 条件不变。每次购物来访记录 0–100 体验分：成交、正面心情、喜欢的陈设与迎宾信任加分；拒绝、负面心情与讨厌的陈设扣分。总分综合平均体验 60%、成交率 25%、开心来访率 15%，并额外扣除不开心来访率 10%，最终限制在 0–100。
- 店员之家的实验排序为：是否通过原年度目标 → 是否经营未亏损 → 顾客满意度 → 钱箱。未亏损要求年末钱箱不低于本年度开始时的钱箱且没有欠薪；它不改变 pass 条件，只是满意度优化的经营安全线。
- 店员之家批量看板逐日显示当前平均士气与 60 分过关线的差距；每局结果表单列记录最终平均士气，使用“54（差 6）”或“67（达标 +7）”格式，便于比较不同策略距离 pass 的稳定程度。
- 店员之家快速实验不生成员工卡片、简历能力说明、士气条和岗位下拉框。AI 在内存中直接写入合法岗位与采集地点；正式游戏和可视化自动玩仍通过原 UI 操作。这样员工数量增加时，批量模拟的 DOM 成本不会随员工卡片数量快速膨胀。
- AI 实验室分别保存“上一批结果模式”和“下一批选择模式”。结果页切换下拉框只改变下一批模式并持久化，不会被上一批的模式值强制改回。
- “本机采用最佳策略”只写入浏览器本地数据；“写入线上策略文件”通过仅监听本机的 `local_server.py` 更新 `ai-policy-defaults.js`，但不会执行暂存、提交或推送。开发者检查文件差异并自行发布后，正式自动玩家按游戏模式读取对应的线上默认策略；玩家本机主动采用的策略仍有更高优先级。
- “下一批选择模式”使用独立的 `late-lantern-ai-next-mode-v1` 本地键；下拉框变化时不再触发整页实验结果重绘，避免上一批模式在同一事件中覆盖用户的新选择。
- 自动玩家存在不可被实验权重覆盖的断供生存底线：连续无交易 3 天后逐日降低开门所需库存覆盖率；第 4/5/6 天的最低覆盖门槛约为 24%/12%/任意可服务来客，且危机阶段深夜必须尝试营业。该规则避免 AI 已经制作商品却因追求高覆盖率整日闭店、自行触发七日无交易失败。
- 10 局分别测试营业覆盖率、危机营业率、招聘现金缓冲、修缮阈值和招募装修偏好等权重。完成后可把最佳权重保存在本机，供普通自动玩家使用；数据不会上传。

## 24. 逻辑覆盖审计索引

本次按最终脚本加载顺序逐项核对，说明书已覆盖以下当前系统：

| 系统 | 主要章节 |
|---|---|
| 五时段、营业／外出、逐日总结 | 0、4 |
| 道路寻路、驻地、散落材料、内部随机路线、并发员工采集 | 0、8、14、23 |
| 日历、28 日月份、84 日季节、336 日年度、月相 | 5、6 |
| 天气、非确定预报、灾害预警 | 7、16.1 |
| 36 位访客、时段权重、空店、好感追加日程 | 3.3、9 |
| 商品发现、现场制作、主动制作、库存与柜台 | 3.2、10、11 |
| 心情、需求数量、报价、陈设和展示影响 | 12、13 |
| 简历、永久进阶、录用、周一入职、工资、休假、欠薪与离职 | 4.1、4.2 |
| 四类员工职位、员工代班、关系、士气、效率和罢工 | 4.3、21 |
| 店铺部件、修补、三阶加固、损坏拒客 | 16.1 |
| 信用、无交易、坚固度失败与失败高光 | 19 |
| 五种开局模式、满意度、年度／剧情胜利 | 20 |
| 可视化自动玩家、资源安全线和关系感知分工 | 18、21 |
| 本地 AI 日志、批量实验、模式排序、精英迭代和线上默认权重文件 | 22、本文末“本地 AI 实验室” |
| 本地存档、重开、WAP 图标、页面入口与响应式交互 | 2、3、14、15、16 |

配置中的逐角色文案、36 份完整人物数值、18 种材料的每条环境权重、24 件商品的逐项配方和所有加固成本不在正文重复抄表；它们分别以 `data/visitors.js`、`data/exploration.js`、`data/items.js` 和 `disaster-system.js` 为唯一数值来源。说明书记录规则结构、关键阈值、状态变化与文件职责，避免复制配置后产生第二份过期数据。
